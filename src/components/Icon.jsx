// Placeholder for Icon component
// This will be replaced with a proper icon library later.
function Icon({ name }) {
  // Return a simple span that displays the icon name, for debugging purposes.
  return <span className="icon-placeholder">[{name}]</span>;
}

export default Icon;