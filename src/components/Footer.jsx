import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>Formatting and navigation controls will be here.</p>
    </footer>
  );
};

export default Footer;