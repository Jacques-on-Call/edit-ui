export function initializeGetQuoteButton() {
  const getQuoteBtn = document.getElementById('getQuoteBtn');
  if (getQuoteBtn) {
    getQuoteBtn.style.display = 'block';
    getQuoteBtn.addEventListener('click', (e) => {});
  }
}
